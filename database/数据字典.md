## 数据库结构数据字典

### 1. 监测站点表 (`sites`)
**说明：** 环境监测站点信息表

| 字段名 | 数据类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| **site_id** | `SERIAL` | **PRIMARY KEY** | 站点内部ID（主键，自动递增） |
| **site_name** | `VARCHAR(100)` | **UNIQUE**, NOT NULL | 监测站点名称 |
| **longitude** | `DOUBLE PRECISION` | NOT NULL | 地理经度 |
| **latitude** | `DOUBLE PRECISION` | NOT NULL | 地理纬度 |

---

### 2. 污染物类型表 (`pollutants`)
**说明：** 污染物类型表

| 字段名 | 数据类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| **pollutant_id** | `SERIAL` | **PRIMARY KEY** | 污染物内部ID（主键，自动递增） |
| **pollutant_name** | `VARCHAR(50)` | **UNIQUE**, NOT NULL | 污染物名称 |

---

### 3. 污染物实时监测数据表 (`measurements`)
**说明：** 污染物实时监测数据（**站点数据**）表（通常用于 CSV 数据导入）

| 字段名 | 数据类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| **distinct_id** | `VARCHAR(50)` | **PRIMARY KEY** | 人工自然主键：由 `date-hour-site_id-pollutant_id` 组合而成 |
| **site_id** | `INT` | **FK** (`sites`), NOT NULL | 监测站点引用ID |
| **pollutant_id** | `INT` | **FK** (`pollutants`), NOT NULL | 污染物类型引用ID |
| **date** | `DATE` | NOT NULL | 采样日期 |
| **hour** | `INT` | NOT NULL, CHECK (0-23) | 采样小时 (0-23) |
| **value** | `DOUBLE PRECISION` | | 监测浓度数值 |
| *(unique_record)* | - | **UNIQUE** | 联合唯一约束：(`site_id`, `pollutant_id`, `date`, `hour`) |

---

### 4. TIF 中提取监测数据表 (`measurements_tif`)
**说明：** tif中提取监测数据表，ftp中获取（自动获取脚本在ftp_download.py中）

| 字段名 | 数据类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| **distinct_id** | `VARCHAR(50)` | **PRIMARY KEY** | 人工自然主键：由 `date-hour-site_id-pollutant_id` 组合而成 |
| **site_id** | `INT` | **FK** (`sites`), NOT NULL | 监测站点引用ID |
| **pollutant_id** | `INT` | **FK** (`pollutants`), NOT NULL | 污染物类型引用ID |
| **date** | `DATE` | NOT NULL | 采样日期 |
| **hour** | `INT` | NOT NULL, CHECK (0-23) | 采样小时 (0-23) |
| **value** | `DOUBLE PRECISION` | | 监测浓度数值 |
| **data_dir** | `VARCHAR(80)` | NOT NULL | tif数据存放路径 |
| *(unique_record2)* | - | **UNIQUE** | 联合唯一约束：(`site_id`, `pollutant_id`, `date`, `hour`) |

### 测试数据：

    -- 插入监测点数据到sites表
    INSERT INTO sites (site_name, longitude, latitude) VALUES
    ('东城东四', 116.417, 39.929),
    ('东城天坛', 116.407, 39.886),
    ('西城官园', 116.339, 39.929),
    ('西城万寿西宫', 116.352, 39.878),
    ('朝阳奥体中心', 116.397, 39.982),
    ('朝阳农展馆', 116.461, 39.937),
    ('海淀万柳', 116.287, 39.987),
    ('海淀四季青', 116.23052, 40.03),
    ('丰台小屯', 116.25528, 39.87694),
    ('丰台云岗', 116.146, 39.824),
    ('石景山古城', 116.176, 39.914),
    ('石景山老山', 116.20764, 39.90886),
    ('昌平镇', 116.234, 40.217),
    ('昌平南邵', 116.27603, 40.21651),
    ('定陵(对照点)', 116.22, 40.292),
    ('延庆夏都', 115.972, 40.453),
    ('延庆石河营', 116.00138, 40.46327),
    ('怀柔镇', 116.628, 40.328),
    ('怀柔新城', 116.6018, 40.3118),
    ('密云镇', 116.832, 40.37),
    ('密云新城', 116.85152, 40.4088),
    ('平谷镇', 117.118, 40.143),
    ('平谷新城', 117.0854, 40.15353),
    ('顺义新城', 116.655, 40.127),
    ('顺义北小营', 116.6853, 40.16087),
    ('通州永顺', 116.67503, 39.93435),
    ('通州东关', 116.6996, 39.9131),
    ('大兴黄村', 116.404, 39.718),
    ('大兴旧宫', 116.47456, 39.78284),
    ('亦庄开发区', 116.506, 39.795),
    ('京东南区域点', 116.78437, 39.63606),
    ('门头沟双峪', 116.106, 39.937),
    ('门头沟三家店', 116.09122, 39.96926),
    ('房山良乡', 116.136, 39.742),
    ('房山燕山', 115.96916, 39.76419)
    ON CONFLICT (site_name) DO NOTHING;
    
    INSERT INTO pollutants (pollutant_id,pollutant_name) VALUES(1,'NO2')--污染物类型数据

检测数据以NO2为例,数据库配置在config.py中。
插入程序中以这两个站点的数据为例：

    ('大兴黄村', 116.404, 39.718),
    ('大兴旧宫', 116.47456, 39.78284)
人工自然主键的生成在以下程序中，生成规则相同所以在进行分析时可以直接根据主键获取对应的value进行分析
-- 对于站点数据 的插入执行./tools/importMeasurements.py
-- 对于tif数据 的插入执行./tools/importTifMeasurements.py
